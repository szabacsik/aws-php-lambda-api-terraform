# Backend overrides for the development environment.
# Values for bucket, region, key, dynamodb_table, and encryption are injected
# by the Makefile during `make tf-init`. Add extra settings here only if needed.
